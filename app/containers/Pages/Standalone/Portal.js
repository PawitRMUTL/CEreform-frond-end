/** @format */

import React from 'react';
import Outer from '../../Templates/Outer';
import Shortcut from '../Users/Shortcut';
function Portal() {
  return (
    <Outer>
      <Shortcut />
    </Outer>
  );
}

export default Portal;
